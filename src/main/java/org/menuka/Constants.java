package org.menuka;

import java.io.File;

public class Constants {
    public static final String path = System.getProperty("user.dir");
    public static final String INSERT_DATA_PATH = path + File.separator + "data" + File.separator + "insert";
    public static final String SEARCH_DATA_PATH = path + File.separator + "data" + File.separator + "search";
    public static final String DELETE_DATA_PATH = path + File.separator + "data" + File.separator + "delete";
}
